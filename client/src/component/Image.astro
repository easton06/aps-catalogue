---
const {entry, width, height} = Astro.props;

let caption = undefined;
let entryArr = entry.split("(");
let image = entryArr[0];
if (entryArr.length > 1) {
  caption = entryArr[1].slice(0, -1);
}
---

<div class="-z-10 relative rounded-xl outline outline-2 outline-indigo-900" style={`width: ${width?? 32}rem; height: ${height?? 32}rem`}>
  <div class="absolute right-0 left-0 bottom-[0.5rem] text-center">
    {caption}
  </div>
  <img class="mx-auto object-center object-contain" style={`width: ${width??  "32"}rem; height: ${caption? height ? height - 3 : 29 : height?? 32}rem`} src={"https://drive.google.com/uc?export=view&id=" + image} alt={entry}>
</div>